version: "3.8"
  services:
  frontend:
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
    - "80:80"
    depends_on:
            - backend
  backend:
    build:
      context: ./backend
        dockerfile: dockerfile
          ports:
          - "8080:8080"